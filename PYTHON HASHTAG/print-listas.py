# Método join -> texto.join(lista)

produtos = ['celular', 'tablet', 'mouse', 'teclado']

print('\n'. join(produtos))  # tecla enter é reconhecida pelo comando "\n"

outrosProdutos = 'celular, tablet, mouse, fone'
lista = outrosProdutos.split(', ')
print(lista)
